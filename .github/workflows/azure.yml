#此工作流将在创建发行版时构建node.js应用程序并将其推送到Azure Web应用程序。N#n#这个工作流假设您已经创建了目标AzureAppServiceWeb应用程序。N#有关说明，请参见https://docs.microsoft.com/azure/app-service/app-service-plan-manage#create-an-app-service-plan n#n#配置此工作流：n#n#1。对于linux应用程序，在下载文件**之前，添加一个名为WebWEBDEPLOY_USE_SCM的应用程序设置，并将其设置为true。N#了解更多说明，请参见：https://docs.microsoft.com/azure/app-service/configure-common#configure-app-settings n#n#2。在您的存储库中设置一个名为Azure_webapp_PublishProfile的秘密，其值为Azure发布配置文件。有关获取发布配置文件的说明，请参见：https://docs.microsoft.com/azure/app-service/deploy-github-actions#configure-the-github-secret n#n#3。更改Azure_webapp_name、Azure_webapp_Package_path和Node_Version环境变量的值(如下所示)。N#n#有关Azure的GitHub操作的更多信息，请参阅https://github.com/Azure/Actions n#，以了解更多要部署到Azure的GitHub操作工作流，请参阅https://github.com/Azure/actions-workflow-samples non：n Relation：n type：[create]n nenv：n Azure_webapp_name：you-app#将其设置为应用程序的名称n Azure_webapp_Package_path：‘.’#将其设置为web应用程序项目的路径，默认为存储库根n节点_Version：‘10.x’#将其设置为使用n个节点版本：n构建和部署：n名称：构建和部署n运行-on：Ubuntu-最新n环境：生产n步骤：n-use：action/checkout@v2n-name：use Node.js${env.node_version}n Use：action/set-node@v2 n with：n节点-版本：${{env.node_version}n-名称：NPM安装，构建和测试n运行：n#构建和测试项目，然后n#部署到Azure Web应用程序。N nnpm安装nnpm运行构建--如果--现在npm运行测试--如果现在n名：“部署到Azure webapp‘n使用：Azure/webapp-Deploy@v2n with：n app-name：${{env.AZURE_webapp_name}n public-profile-配置文件：${{inv.AZURE_webapp_public_profile}n包”${{env.AZE_webapp_Package_path}
